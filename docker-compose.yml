version: '3.8'

services:
  money-tracker:
    build: .
    container_name: money-tracker
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - data_volume:/app/data
    environment:
      - PORT=3000
      - NODE_ENV=production

volumes:
  data_volume:
